import React, { Component } from 'react';

class Info extends Component {
  constructor() {
    super();

    this.state = {
    };
  }

  onSubmitForm = (e) => {
    console.log();
  };

  render() {
    return (
      <form action="#" onSubmit={this.onSubmitForm}>
        <fieldset>

        </fieldset>
      </form>
    );
  }
}

